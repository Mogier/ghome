	<h3>Actionneurs</h3>

	<table id="table_actionneurs" class="table table-bordered table-hover table-striped">
		<thead>
			<tr id="search">
				<th style="width:10%">Details</th>
				<th>Numero</th>
				<th>Espace</th>
				<th style="width:10%"> </th>
			</tr> 
		</thead>					  
		<tbody id="actionneurs">	
		{% for act in actionneurs %}
				<tr id="{{ act.id }}">
					  <td class="text-center"> <span class="badge"> <span onclick="fnDetails(this);" class="glyphicon glyphicon-plus"></span> </span> </td>
					  <td> </td>
					  <td>{{ act.idEspace.id }}</td>
					  <td> <a type="button" class="btn btn-danger btn-sm" href="/ghome/site/web/app_dev.php/deleteSpace/{{act.id}}">Suppr</a></td>
				</tr>	
		{% endfor %}
		</tbody>
	</table>
<!--
	<button class="btn btn-primary btn-lg" data-toggle="modal" data-target="#myModal">
	Ajout Espace
	</button>

	<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
		<div class="modal-dialog">
			<div class="modal-content">
				<div class="modal-header">
					<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
					<h4 class="modal-title" id="myModalLabel">Ajouter un espace</h4>
				</div>
					<div class="modal-body">

						<button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
					</div>
				</div>
			</div>
		</div>
-->		
		<script type="text/javascript">
			function fnFormatDetails ( oTable, nTr )
			{
				var sOut = '<button style="width:30%" type="button" class="btn btn-success">On</button> <button style="width:30%" type="button" class="btn btn-danger">Off</button>';
				
				return sOut;
			}


    		var table = $('#table_actionneurs').dataTable();

    		function fnDetails(element) 
    		{
				var nTr = element.parentNode.parentNode.parentNode;
				if ( element.className == 'glyphicon glyphicon-minus' )
				{
					/* This row is already open - close it */
					element.className="glyphicon glyphicon-plus";
					table.fnClose( nTr );
				}
				else
				{
					/* Open this row */
					element.className="glyphicon glyphicon-minus";
					table.fnOpen( nTr, fnFormatDetails(table, nTr), 'details text-center' );
				}
			}	

		</script>